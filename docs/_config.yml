title: Fridge Dev Blog
author:
  name: fridge-dev
  email: todo@domain.com

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  Software engineering blog covering topics like programming patterns,
  code readability, maintaining a software project as a team, self
  analysis habits, and communication skills. Blog posts are short
  and to the point, especially soft-skill, stream-of-conciousness posts.

# Build settings


# As of November 2023, GitHub Pages still uses Minima 2.5.1 (https://pages.github.com/versions/).
# To use minima v3, we use remote theme
#theme: minima
remote_theme: jekyll/minima

plugins:
  # From minima v3:
  - jekyll-seo-tag
  # Gen AI prompt: "github pages how to redirect from one markdown file to another"
  - jekyll-redirect-from

# Theme-specific settings

# If you want to link only specific pages in your header, use this and add the path to the pages
# in order as they should show up.
header_pages:
  - about.md

# Set to `true` to show excerpts on the homepage.
#show_excerpts: false

# Minima specific settings, which are only available from Minima 3.0 onward.
minima:
  # Minima skin selection.
  # Available skins are:
  # classic            Default, light color scheme.
  # dark               Dark variant of the classic skin.
  # auto               Adaptive skin based on the default classic and dark skins.
  # solarized-light    Light variant of solarized color scheme.
  # solarized-dark     Dark variant of solarized color scheme.
  # solarized          Adaptive skin for solarized color scheme skins.
  skin: solarized

  # Generate social links in footer.
  social_links:
    - { platform: github, user_url: "https://github.com/fridge-dev/blog" }
